{{#partial "styles"}}
    <link rel="stylesheet" href="/css/heroesList.css">
{{/partial}}
{{#partial "content"}}

    <h1 class="mt-5 text-center">Squad List</h1>

    {{#if squads}}
        {{#each squads}}
            <div class="container mt-5 w-50 list">
                <ul>
                    <li>Squad Name: {{name}}</li>
                    <li>Maximum Size: {{maxSize}}</li>
                    <li>Squad Cause: {{cause}}</li>
                </ul>
            </div>
        {{/each}}
        <p class="text-center mt-4"> <a href="/squads"><button type="button" class="btn">Add Squad </button></a></p>
    {{else}}
        <p class="mt-5 text-center">Nothing to see here </p>
    {{/if}}
{{/partial}}
{{>layout.hbs}}